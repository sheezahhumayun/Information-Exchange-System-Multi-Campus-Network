# Information-Exchange-System-Multi-Campus-Network
The Campus Communications System (CCS) is a hybrid networking application built in C++ that uses TCP for reliable messaging and file transfer, and UDP for real-time heartbeats and admin broadcasts, all managed by a concurrent, multithreaded central server.
